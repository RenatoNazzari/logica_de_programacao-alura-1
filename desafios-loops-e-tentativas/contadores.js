// Primeiro contador

/* let contador = 1;

while (contador <= 10) {
    console.log(`Seu número é ${contador}`);
    contador++;
} */

// Segundo contador

/* contador = 10;

while (contador >= 0) {
    console.log(`Seu número é ${contador}`);
    contador--;
}
 */

// Contagem regressiva

/* let numeroInicial = prompt("Digite o valor para a contagem regressiva aqui:");

while (numeroInicial >= 0) {
    console.log(contador);
    contador--;
}
 */

//Contagem progressiva

/* let numeroFinal = prompt("Digite o valor para a contagem progressiva aqui:");

let contador = 0;

while (contador <= numeroFinal) {
    console.log(contador);
    contador++;
} */